{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "port": 8080,
            "request": "attach",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        },
        {
            "name": "Launch Chrome(RotationVisualizer)",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5500/RotationVisualizer.html",
            "webRoot": "${workspaceFolder}"
        },
    
        {
            "type": "chrome",
            "request": "launch",
            "name": "localhost に対して Chrome を起動",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}"
        }
    ]
}